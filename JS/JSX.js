/**
 * --------------------------------------------
 * JSX spread operator
 * --------------------------------------------
 */
const recipes = ['list of recipes'] 

// { props.recipes.map((recipe, i) =>
//     <Recipe key = {i} {...recipe} />
// )}

/**
 * ---------------------------------------------
 * we can use destructuring object as parameters
 * instead of props. 
 * ---------------------------------------------
 */

 /**
  * -----------------------------------------------
  * main benefits of modular bundling (eg with webpack)
  * is modularity, network performance
  * 
  * roll ups or layers
  * -----------------------------------------------
  */

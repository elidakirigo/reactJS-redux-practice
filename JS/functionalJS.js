/**
 * ---------------------------------------------------
 * if youve mapped, or used the reduce function . you
 * have used javascript as functional javascript.
 * ---------------------------------------------------
 */

/**
 * -----------------------------------------------
 * changing a string to a URL using replace
 * -----------------------------------------------
 */

const myString = "This is the mid day show with Cheryl Waters"
const urlFriendly = myString.replace(/ /g, '-');
console.log(urlFriendly);
